apiVersion: v1
kind: Service
metadata:
  name: bookwork-frontend
  namespace: bookwork
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: bookwork-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  